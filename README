BUGS:
  - returns {"@runs":[{"@run":{...}]} instead of {"runs":[{"run":{...}]}
  - handle situation when has_one association is nil, e.g. it simply should return nothing instead of giving the error
TODO:
  - option to generate keys without quotes
  - option to avoid rails noise {"runs":[{"run":{"id":"1", ...}}]} 
    Make it propuce {"runs":[{"id":1}]} instead
  - gluing for mas_many associations
  - comments //
  - :plain (some symbol for this?)
  - :only            =>         .aaa
      .aaa                      .bbb
      .bbb
  - :except          =>         !aaa
      .aaa                      !bbb
      .bbb
  -  :plain by default
  -  define :all by symbol
  